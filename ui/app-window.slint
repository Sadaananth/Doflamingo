import { Date, DatePickerPopup, Button, VerticalBox } from "std-widgets.slint";
import {DebtTab} from "debt.slint";
import {SalaryTab} from "salary.slint";
import {HomeView} from "home.slint";

export global Data {
    in-out property <int> today: 23;
    in-out property <int> this_month: 12;
    in-out property <int> this_year: 2001;
}

export component AppWindow inherits Window {
    width: 800px;
    height: 600px;

    callback add_salary(string, int);

    salary := SalaryTab {
        visible: false;

        add_salary(date_set, salary_set) => {
            root.add_salary(date_set, salary_set);
        }
    }

    debt := DebtTab {
        visible: false;
    }

    homeview := HomeView {
        enter_salary => {
            self.visible = false;
            salary.visible = true;
        }

        enter_debt => {
            self.visible = false;
            debt.visible =  true;
        }
    }
}
